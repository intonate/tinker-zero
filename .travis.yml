language: php
dist: trusty
sudo: false

php:
  - 7.0
  - 7.1
  - 7.2

before_script:
  - phpenv config-rm xdebug.ini
  - composer install --no-interaction --prefer-stable --no-suggest

script:
  - ./vendor/bin/phpunit

notifications:
  email:
    recipients:
      - scrub.mx@gmail.com
